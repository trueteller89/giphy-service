# Giphy service

## Prerequisites

* Node version >= 5.0
* Gulp

## Installation 

* `npm install`
* install bower if missing (`npm install -g bower`)
* `bower install`

## Build & Start the application


* `gulp` (starts on port 8000)

